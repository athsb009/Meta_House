/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // Enable dark mode via a class on the root element (typically <html> or <body>)
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
